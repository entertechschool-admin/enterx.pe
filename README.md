# enterx.pe

Sitio web público de **EnterX** — la unidad de IA aplicada B2B de Enter Tech School (respaldo CETEMIN).
Construido con Next.js + Tailwind CSS.

## Para el agente implementador

Lee en orden: **`SPEC.md`** (qué construir) → **`CONTEXT.md`** (contenido + marca) →
`brand/reference/manual-de-marca.html` (referencia visual). Las reglas de trabajo están en `CLAUDE.md`.
